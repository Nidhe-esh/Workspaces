"""
scraper.py — Workspaces
Scans running Windows processes and returns structured app data.

STATUS: STUB — will be implemented in Step 1.
app.py falls back to mock data if this module is missing or incomplete.

When implemented:
  pip install psutil pygetwindow

Usage:
  from scraper import scan_workspace
  result = scan_workspace()  # → { "apps": [...] }
"""


def scan_workspace() -> dict:
    """
    Placeholder. Replace this entire function in Step 1 with real
    psutil + pygetwindow logic.
    """
    raise NotImplementedError(
        "scraper.py not yet implemented. "
        "app.py will use mock data until Step 1 is complete."
    )
